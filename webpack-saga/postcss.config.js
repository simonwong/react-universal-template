/* eslint-disable import/no-extraneous-dependencies */
module.exports = {
  plugins: [
    // autoprefixer用来处理css前缀的
    // eslint-disable-next-line global-require
    require('autoprefixer'),
  ],
}
